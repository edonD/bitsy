VERDICT: FAIL

---
verdict: FAIL
score: 9/10
round: 1
---

### 1. Builds Clean: 2/2

`npm run build` succeeds with zero errors, zero warnings. No TypeScript errors. All routes compile and generate static pages correctly. The calculator page at `/calculator` is 139 B with 98.1 kB first load JS — reasonable size. No console errors expected at runtime based on code review.

### 2. Content Accuracy: 1/2

**Pricing data is accurate.** Every model's `costPerQuery`, `inputPer1M`, and `outputPer1M` in `CostCalculator.tsx` matches the approved Research 2.3 exactly:
- GPT-4.1-nano: $0.10/$0.40 per 1M → $0.0002/query ✓
- Claude Haiku 4.5: $1.00/$5.00 per 1M → $0.0026/query ✓
- Perplexity Sonar: $1.00/$1.00 per 1M + $0.008/request (medium context) ✓
- All other models verified ✓

The optimization strategies section accurately represents the research: tiered (90-95%), batch (50%), prompt caching (50-90%), semantic caching (30-73%).

**CRITICAL BUG: The "Compared to GEO SaaS Tools" section is mathematically wrong.** It multiplies `results.queriesPerMonth` by the SaaS per-prompt cost. But `queriesPerMonth` includes the model × sample amplification factor:

```
queriesPerMonth = brands × queriesPerBrand × modelCount × samplesPerQuery × pollingFrequency × 30
```

SaaS tools define a "prompt" as a single user-level query (e.g., "best CRM for small business"), NOT the total API calls across all models and samples. When Otterly says "15 prompts" in their $29/mo plan, that means 15 queries to their platform — they handle multi-model querying internally.

**The correct SaaS comparison base** should be: `brands × queriesPerBrand × pollingFrequency × 30` (the number of unique user-level queries).

**Example with default settings (10 brands, 5 queries, 4 models, 3 samples, 1 poll/day):**
- Calculator's `queriesPerMonth` = 18,000
- Calculator shows: Otterly.AI: ~$27,000/mo at ~$1.50/prompt
- Correct SaaS prompt count: 10 × 5 × 1 × 30 = 1,500 prompts
- Correct Otterly comparison: 1,500 × $1.50 = $2,250/mo

The SaaS comparison is **inflated by 12x** (4 models × 3 samples) for default settings. This makes the DIY approach look dramatically cheaper relative to SaaS than it actually is. The "10x to 1,000x markup" claim at the bottom of the section is also wrong because it's comparing apples (total internal API queries) to oranges (SaaS user-level prompts).

### 3. Usability: 2/2

Excellent interactive experience:
- Range sliders with live value labels
- Quick presets for common configurations (Startup MVP through Enterprise)
- Clean two-column layout: inputs left, results right
- Summary cards for key metrics (queries/month, queries/day, monthly cost, annual cost)
- Tiered strategy shows savings percentage dynamically
- Per-model breakdown table with token cost / request fees / total columns
- Contextual recommendations that change based on spend level
- SaaS comparison section (content bug aside, the UX is good)
- A non-technical user could navigate and understand the output

### 4. Code Quality: 2/2

Clean, well-structured component:
- `ModelInfo` interface properly typed with all relevant fields
- Data array `MODELS` is clean configuration — appropriate for hardcoded pricing data
- `useMemo` correctly memoizes the calculation with all dependencies
- Tiered strategy logic handles edge cases (fallback models when tier is empty)
- `formatCurrency` handles different magnitude ranges appropriately
- Single component at ~630 lines is acceptable for a self-contained calculator
- No `any` types, no code duplication, no dead code
- The `compact` prop allows reuse in embedded contexts

### 5. Completeness: 2/2

Task spec requires:
- **Input: number of queries, models, samples/day** ✓ — Has brands, queries per brand, samples per query, polls per day, model selection checkboxes
- **Output: estimated monthly cost** ✓ — Monthly cost, annual cost, per-model breakdown, queries/day, queries/month, average cost per query
- **Output: recommended config** ✓ — Dynamic recommendation section with contextual advice based on spend level

Beyond the spec: presets, optimization toggles (batch API, tiered strategy), SaaS comparison, explanation of cost methodology, optimization strategies section, pricing assumptions, source list. No TODOs, no placeholders, no stubs.

---

### Issues That MUST Be Fixed (ordered by severity):

1. **[CRITICAL] SaaS comparison uses wrong query count.** Lines 607-610 in `CostCalculator.tsx` multiply `results.queriesPerMonth` (which includes model × sample amplification) by SaaS per-prompt costs. SaaS "prompts" are user-level queries, not individual API calls. The comparison should use `brands × queriesPerBrand × pollingFrequency × 30` as the base for SaaS cost estimates. Compute this as a separate value (e.g., `promptsPerMonth`) in the `useMemo` block and use it in the SaaS comparison section.

2. **[CRITICAL] The "10x to 1,000x markup" claim is wrong.** Because the comparison base is inflated, the markup ratio is also inflated. After fixing the comparison base, recalculate and state the actual markup range. Per Research 2.3, the real markup is more like 1.5x-50x depending on the tool (Sellm at ~$0.01/prompt vs raw API at ~$0.0002-0.008/query is a much more nuanced comparison).

### Research Paths for Next Round:

- **GAP: Incorrect SaaS comparison math**
  - No additional research needed — this is a code fix
  - **What to do**: Add a `promptsPerMonth = brands * queriesPerBrand * pollingFrequency * 30` variable. Use this (not `queriesPerMonth`) when computing SaaS tool costs. Update the descriptive text to explain that SaaS tools charge per user-level prompt while the calculator's total reflects internal API queries across all models and samples.
  - **Why it matters**: The comparison is the most decision-relevant part of the calculator — it tells users whether DIY or SaaS is more cost-effective. Getting this wrong undermines the entire tool's credibility.

- **GAP: Markup claim needs correction**
  - After fixing the base, compute actual markup: for a typical config (10 brands, 5 queries, 4 models, 3 samples, daily), raw API cost is ~$X/mo and SaaS equivalents would be ~$Y/mo at the corrected prompt count.
  - The text should explain that the markup varies significantly by tool and that the "value add" (parsing, dashboards, insights) accounts for some of the premium.

### What Was Done Well (be brief — max 3 bullets):

- Pricing data meticulously matches Research 2.3, including per-request fees for Perplexity and batch discount modeling
- The tiered strategy calculation is genuinely useful — it correctly computes savings by allocating 90%/9%/1% across budget/mid/flagship tiers using the cheapest available model in each tier
- Presets and contextual recommendations make this usable for non-technical stakeholders, not just engineers

### Prediction for Next Round:

The fix is surgical — add one computed value (`promptsPerMonth`) and update 6-8 lines in the SaaS comparison section. Worker should be able to pass in Round 2 if they correctly distinguish between internal API queries and SaaS-level prompts.
